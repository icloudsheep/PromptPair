我希望做一个自用的以及可以分享的Prompt分享网站。要求可以实现以下功能：

1. 分门别类，有角色、前端、后端知识库、代码规范等各种分类
2. 最根本的数据结构为多个 配置 文件，例如assets文件夹下prompt文件夹下有frontEnd.xxxx 等文件。这些文件中按照一定的规则部署各种场景下的prompt，所有的网页元素文字都应该在可以由人类识读的文件中表示
3. 拥有dark mode
4. 主题文件（CSS类似物）应该在专门的文件夹中
5. 代码文件（JS类似物）应该在专门的文件夹中
6. 用户选择Prompt的操作全部来源于Prompt融合功能，用户可以在不同的类别中选择不同的Prompt积木，将这些积木拼接在一起，类似于Scratch一样
7. 用户拼接后，点击生成，整个积木结构将会根据根目录.env的大模型API路径和Key发送并生成最终的Prompt
8. 整个项目应该是模块化可扩展的，例如日后我会让你增加设置功能、主题功能、模型功能，请预留模块化接口和升级可能性

如有任何问题，随时向我提问，每次提问仅可以提一个问题并且给出你认可的参考答案

---

## 已固化的项目约定（2026-07-29）

- 项目采用 Python 标准库服务端与浏览器原生 ES Modules，不依赖构建工具。
- 模型密钥只从根目录 `.env` 读取，前端配置和配方代码不得包含密钥。
- Prompt 数据、界面文案、主题样式和功能代码分别存放，增加 Prompt 分类不应要求修改 JavaScript。
- 模型服务根据 `.env` 基址自动使用 OpenAI 兼容的 `chat/completions` 或 Anthropic 兼容的 `v1/messages` 接口，浏览器只调用本项目的代理接口。
- Prompt 配方通过人类可读的 JSON 代码分享，不修改 URL；只有用户点击生成时才发送积木内容。
- 后续设置、主题和模型能力以独立模块接入，避免逻辑集中到单一入口文件。

---

新的要求如下：

1. 积木的表型形式应该更加风丰富，比如角色应该作为一个中空的积木将其他要求包裹在内，当选择多个角色时，多个角色应该融合（可以引入积木类型，每一块积木都需要有该属性）
2. 当前网站的部分文字字体过小，导致模糊
3. 分享的大小异常
4. 引入设置页面，目前可以设置语言、可视化编辑所有积木（注意页面层级避免杂乱），编辑后需要实时写入实时生效
5. 我已经填好了.env文件

如有任何问题，随时向我提问，每次提问仅可以提一个问题并且给出你认可的参考答案

---

## 第二阶段固化约定（2026-07-29）

- 每块 Prompt 配置必须具有 `type`，当前只允许 `role`、`instruction`、`context`、`constraint`、`quality`。
- 所有 `role` 积木在画布中融合为一个中空外层角色容器；其他类型积木位于容器内部。多个角色表示一个复合角色，不执行互相替换。
- 设置页面使用“设置分区 → 分类 → 积木 → 编辑表单”的层级。编辑经过 500 ms 防抖后自动保存，且同一分类的请求必须串行，避免较旧响应覆盖较新输入。
- 设置接口只写入 manifest 白名单中的 Prompt JSON 与 app 配置中已登记的语言，不读取、展示或修改 `.env`。
- 配置保存成功后，积木库实时更新。画布实例若仍等于旧的源 Prompt，则跟随配置更新；用户已手动改写的正文继续保留。
- 配方代码只支持当前 `version: 1` JSON 结构，不兼容 URL hash 或历史格式。默认正文不进入配方，只有用户改写过的正文才写入 `content`。

---

## 第三阶段固化约定（2026-07-29）

- 项目尚未上线，不保留旧分享格式和旧界面行为的兼容分支。
- 分享配方只通过弹窗中的人类可读 JSON 代码导入与导出，不得修改页面地址。
- 工作台中的积木实例使用持久 DOM 和增量协调；新增、删除、排序及生成状态变化不得重建全部积木节点。
- 所有界面状态变化使用非线性动画，并遵守系统的减少动态效果设置。
- 工作台采用纯平面实色色块，不使用渐变、模糊背景或半透明表面。
- 角色积木使用 Scratch 风格的不规则中空外壳；空状态只保留窄槽，加入其他积木后槽位随内容自然扩张。
- 设置导航名称为“积木”，设置内容和编辑表单统一左上对齐；原生选择框由项目主题完整接管视觉样式。

---

新的要求如下：

1. 目前从未上线，现在无需考虑任何兼容性
2. “分享配方”按钮在点击后，本地的网址链接会发生变更，不应该发生这类变更，配方应该以代码的形式，在输入框输入而非在网址中输入
3. “分享配方”按钮的大小显著大于“清空”按钮，应该改为和“清空”的高度一致，iconfont和描述文字横向布置
4. 增加动画，所有变化都应该支持非线性动画
5. 当前增加一块积木时整个工作台的所有积木都会刷新，不应该这样
6. 角色积木是不规则的中空圆角矩形，放下时中间空出一条线，此时增加其他类型的积木时，中间的空间扩大，然后无缝嵌入中间的空间。
7. 工作台的视觉风格时纯平面的，使用色块，不要使用半透明元素，更类似于现实中的积木或者类似于Scratch一样
8. 设置-常规中，语言下拉框的样式也应该我们自己写
9. 设置-Prompt积木中，名称改为积木，和常规对齐，编辑界面应该左上对齐而不是目前的居中
